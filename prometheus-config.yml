global:
  scrape_interval: 15s

scrape_configs:
  - job_name: 'server_metric'
    metrics_path: '/actuator/prometheus'
    static_configs:
      - targets: [ 'host.docker.internal:8080' ]
        labels:
          instance: 'prod_server'

